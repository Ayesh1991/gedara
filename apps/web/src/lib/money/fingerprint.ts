// The ledger-v7 fingerprints live with the drive-scan Edge Function (supabase/functions/_shared/scan),
// so the function and the app compute the same ids (CLAUDE.md rule 5).
export * from '@scan/fingerprint';
