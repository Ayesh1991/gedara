# Decisions log

Format: date · decision · why. Append-only.

- 2026-09-23 · Project name **Gedara (ගෙදර)**; slug `gedara` everywhere · final name
- 2026-09-23 · Backend Supabase on a **separate account (ayeshmantha1991.24@gmail.com)**, projects `gedara-staging` + `gedara-prod`, region Singapore · main account is used by another project; closest region
- 2026-09-23 · Hosting Vercel on `gedara.vercel.app` (fallback `gedara-home`), **no custom domain** · free; QR plan in MASTER_PLAN §7d
- 2026-09-23 · Household model; users Didula Ayeshmantha (ayeshmantha@gmail.com, owner) and Sandeepani Thennakoon (asithaathennakoon@gmail.com, member); public sign-up disabled · two-person household with invite-only access
- 2026-09-23 · Auth = email 6-digit OTP (not magic link) · magic links open Safari, not the installed iOS PWA
- 2026-09-23 · Labels: NIIMBOT B1 **20×20 mm** (raw code only, QR v1-M alphanumeric) + Epson L3110 A4 **6×9 = 54** (URL QR) · owned/purchased hardware
- 2026-09-23 · Photos: Supabase thumbnails + full size until ~70 % full; Google Drive overflow for large files/docs · stay within free tiers
- 2026-09-23 · Kitchen Scale Station on Raspberry Pi 2 (HX711 5 kg + USB scanner + NIIMBOT print service) as Phase 6b · exact consumption of loose goods
- 2026-09-23 · Auth emails sent via **Gmail SMTP** (ayeshmantha1991.24@gmail.com, App Password) in both projects · Supabase default SMTP only reaches org team members, 2/hour
- 2026-09-23 · Vercel Root Directory = `apps/web` · never publish docs/reference as static files
- 2026-09-23 · React 19 / Vite 8 / Tailwind 4 / TypeScript 6.0 instead of MASTER_PLAN's "React 18" · current shadcn/ui + TanStack target React 19; TS pinned < 6.1 because typescript-eslint doesn't support TS 7 yet
- 2026-09-23 · `app_meta` is the one table without `household_id` (RLS on, no policies, read only via `schema_version()`) · global build metadata, nothing household-specific
- 2026-09-23 · `schema_version` = integer bumped by the last statement of every migration · the badge shows exactly which migration a DB has
- 2026-09-23 · Service worker via vite-plugin-pwa **injectManifest** (own `src/sw.ts`), not generateSW · generateSW's precache is cache-first; ours is network-first with caches as offline fallback only
- 2026-09-23 · APP_VERSION = `apps/web/package.json` version, scheme `0.<phase>.<deploy>` · version tells you the phase at a glance
- 2026-09-23 · Tenancy tables are read-only to clients in Phase 0; membership only via `household_invite` + auth trigger · invite-only household
- 2026-09-23 · No Docker: SQL tests run against gedara-staging via `scripts/sql-test.mjs` → `supabase db query --linked` (Management API; each test ends by raising its TAP output, so the transaction always aborts — nothing persists; refuses unless linked ref = staging; `--with-migrations` proves new migrations before `db push`); e2e logs in with an admin-minted OTP (`generateLink`, no email) and refuses non-staging URLs · develop against staging, never prod
- 2026-09-23 · supabase-js pinned to 2.116.0 (not 2.117.1) · pnpm 12 minimum-release-age gate; don't bypass it for day-old releases
- 2026-09-23 · RLS helpers live in schema `private` (`private.is_member`, `private.can_write`, `private.is_owner`), moved there by migration 6 · Supabase advisor 0029: SECURITY DEFINER helpers shouldn't be callable as /rest/v1/rpc; cheaper to fix before dozens of policies exist
- 2026-09-23 · Accepted advisor warnings: `schema_version()` executable by anon/authenticated (intentional — badge shows DB version on the login screen) · leaked-password protection off (OTP-only, no passwords)
- 2026-09-23 · gedara-staging was created in **Tokyo (ap-northeast-1)**, prod is Singapore · staging-only latency cost; not worth recreating now
- 2026-09-23 · Email OTP Length set to **6** in both Supabase projects (was 8) · app + email template expect a 6-digit code; an 8-digit code was silently truncated
- 2026-09-23 · e2e runs on the system **Microsoft Edge** (`E2E_CHANNEL=msedge`) on the dev PC · Playwright's Chromium 1243 fails to start on this Windows 10 machine (side-by-side error)
- 2026-09-23 · Design language **Aurora** replaces the ledger v7 palette (void #05070F, glass, violet/cyan/teal aurora; gold #F5B83D kept for the single primary action) · approved from the design preview; CLAUDE.md rule 11 updated
- 2026-09-23 · User-selectable aurora themes (aurora, nebula, lagoon, ember, mono) + Motion (system/on/off), stored in Supabase Auth `user_metadata.prefs` and mirrored to localStorage for a flash-free first paint · per user, synced across devices, no new table/RLS; themes never change gold or status colours so meaning is stable
- 2026-09-23 · Real app shows empty states (ghost charts, "arrives in Phase N"), never sample numbers · a finance app must not display invented amounts
- 2026-09-23 · Appearance prefs carry `updatedAt`; on start-up the newer of device vs account wins and a newer local choice is re-pushed · a save interrupted by a reload/navigation would otherwise be lost or overwritten by stale account data
- 2026-09-23 · Web build order is `vite build && tsc -b --noEmit` · the TanStack Router plugin generates routeTree.gen.ts during the Vite build, so new route files are unknown to tsc until then
- 2026-09-24 · HL codes are generated by the database (`private.new_hl_code`, Crockford base32 from `gen_random_bytes`), unique across all households and immutable (no client INSERT/UPDATE privilege on `code`) · printed labels must never change meaning or collide between households
- 2026-09-24 · A place's parent must be in the same household via a composite FK `(household_id, parent_id) → location(household_id, id)`; cycles rejected by trigger; `path` kept by triggers · the tree can't be corrupted by the client
- 2026-09-24 · Place photos live in the general `attachment` table (one primary per entity), not a `location.photo_path` column · §3.7 wants one table for every file; Phase 5 reuses it for warranties/manuals
- 2026-09-24 · Deleting a place: the DB refuses while anything is inside; the UI hides it at once and deletes after an 8 s Undo window (committed immediately if the app goes to the background) · §5.4 Undo without a soft-delete column
- 2026-09-24 · NIIMBOT labels in Phase 1 = downloaded 1-bit 203 dpi PNGs printed from the NIIMBOT app; direct printing waits for the Pi station (6b) · §7c fallback, no extra hardware now
- 2026-09-24 · Camera scanning uses native BarcodeDetector where available, else zxing-wasm with its `.wasm` bundled by Vite (never jsDelivr); WebP encoding falls back to @jsquash/webp WASM (Safari) · works on iPhone/Windows, offline-capable, no third-party CDN
- 2026-09-24 · A4 label calibration is stored per household in `label_profile` (defaults in code until saved) · feed offsets belong to the printer, shared by both users
- 2026-09-24 · Label-PDF code (pdf-lib + fontkit, ~1 MB) is a lazy chunk excluded from the service-worker warm-up · don't make every phone download it on each deploy
- 2026-09-24 · iPhone camera QR scans open Safari, not the installed PWA (same as magic links): sign in once in Safari and labels open straight to the place; Android opens `/s/` links in the installed app · platform limitation; A4 labels still work everywhere
- 2026-09-24 · Login keeps a validated same-origin `redirect` (`/login?redirect=/s/…`) · a scanned label must land on the box after signing in
- 2026-09-24 · Print real labels from **production** after merge: A4 QR URLs point at `VITE_PUBLIC_BASE_URL` (gedara.vercel.app → prod DB); preview/staging codes don't exist in prod · labels are permanent
- 2026-09-24 · Phase 2 scope = Money core only; bank-SMS import is Phase 2b · keep the ledger migration and SMS parsing separately reviewable
- 2026-09-24 · Money tables are read-only to clients; every write goes through SECURITY DEFINER RPCs (`rpc_save_transaction`, `rpc_import_bills`, `rpc_delete_transaction`, `rpc_move_transactions`) that check `private.can_write` · header + lines are written atomically and "lines add up to the total" is enforced in one place (advisor 0029 accepted for these, as for the stock RPCs to come)
- 2026-09-24 · Balances are never stored: `v_account_balance` = opening balance at the END of `opening_on` + transactions after it; cards carry a negative balance (= owed), available = limit + balance; later corrections are `adjustment` transactions (new type) · rule 1; history before an account's opening day counts as spending but not towards its balance
- 2026-09-24 · Fingerprint prefixes: `b` scanned bills (ledger v7, byte-for-byte), `L` older Sheet rows without v7 ids (hash of their row ids; lines keep the Sheet id), `m` manual entries (hash of type|date|time|account|payee|total, `~n` when saved again on purpose), transfer fees `<transfer>~fee` · deterministic everywhere (rule 5); a double tap asks "save another?"
- 2026-09-24 · `transaction_line.line_no` is 0-based = the ledger v7 line index · the index is part of the v7 line fingerprint
- 2026-09-24 · A scanned bill whose items don't add up to its total gets one extra line (discount / rounding / charges) in the biggest line's category · "every rupee lives in transaction_line"
- 2026-09-24 · Ledger Sheet migration runs in the app (Money › Import › Old ledger Sheet, CSV → `rpc_import_bills` as the signed-in user) instead of `scripts/import-sheet.ts` · no service-role key needed for prod, a click for Didula, re-running is harmless
- 2026-09-24 · Sheet import skips the 2 `TEST-` rows and the second upload of Invoice 299 (Cargills 27 Jun, Rs 595.00 counted twice in the Sheet); reconciliation shows Sheet 204,613.61 = Gedara 204,018.61 + skipped 595.00 · the Sheet itself double-counted that bill
- 2026-09-24 · Sheet rows paid by "card" go to the suspense account "Card — to be matched"; moved to the right card by hand (bulk "Move") now, by SMS matching in 2b · the Sheet never said which card
- 2026-09-24 · System `unit` table (g, kg, ml, L, pcs, pack …, read-only, `household_id` null) created in Phase 2 instead of 3; a line trigger fills `unit_id`, `base_qty`, `price_per_base` · rule 4 from the first bill (Rs/kg for vegetables); Phase 3 adds household units + product conversions
- 2026-09-24 · Seeded for the Gedara household: ledger v7 categories + Income (Salary, Private practice, Interest, Gifts received, Other income) and the accounts Cash, BOC Savings ..319 (Rs 6,443.81 at end of 2026-09-24), BOC/Seylan/Sampath/People's credit cards with limits, Card — to be matched · from Didula's notes; card and cash starting balances are set in the app
- 2026-09-24 · Budgets are not in any §9 phase yet; the Home "Budget burn" card stays an empty state · propose them with Phase 6 Insights
- 2026-09-24 · Bank SMS arrive via the open-source Android app **SMS to URL Forwarder** (F-Droid, MIT) posting to the `sms-ingest` Edge Function with a per-device secret header · sender regex + text regex on the phone, custom headers, retries; no Play-Store SMS-permission issues, no paid gateway
- 2026-09-24 · Every alert lands in a **review inbox** (`sms_message`), never straight into the ledger; one-tap "Accept clear matches" only for exact links and transfer pairs · sender ids can be spoofed and parsers can be wrong; money entries must be confirmed by a person
- 2026-09-24 · OTP / PIN / password texts are dropped on the phone, in the browser (backup import), in the Edge Function and again in SQL (`private.sms_is_secret`); they are never stored, only counted · defence in depth for the one kind of SMS that must never leave the phone
- 2026-09-24 · Device tokens: 32 random bytes shown once, only sha256 stored, column not readable by clients; owner-only add/remove; the device path is the only place the service role is used (one RPC granted to service_role only) · the phone has no Supabase session; a leaked token can only add inbox rows and is revoked in one tap
- 2026-09-24 · Inbox fingerprint `s<djb2(sender|body|received minute)>`; the same sender + text within 24 h is also a duplicate; Seylan txn ids are unique · the live forwarder and a later SMS-backup file stamp the same SMS seconds apart; rule 5 (deterministic)
- 2026-09-24 · Inbox status is derived (ignored / has transaction_id / new), not stored; deleting a transaction returns its alerts to the inbox (FK on delete set null) · no status can drift from the ledger
- 2026-09-24 · Alerts map to accounts by `account.institution` + `last4` (BOC savings "319" vs BOC card "8873" share the sender) · no separate sender-mapping table; fixed in account settings
- 2026-09-24 · Matching suggestions are computed in the web app (`lib/sms/match.ts`, unit-tested); the database only enforces household + "not reviewed twice" (GDLNK) · rules will be tuned with real data; keeping them out of migrations keeps them cheap to change
- 2026-09-24 · Bank parsers live once in `supabase/functions/_shared/sms/parse.ts` (plain TS, no imports) and the web app imports them via the `@sms` alias · one parser for phone, backup and tests
- 2026-09-24 · `rpc_save_transaction` became a wrapper around `private.save_transaction(p, source)` (migration 20, create or replace) so SMS posts are `source = 'sms'` · same checks/fee logic for manual and SMS entries; the applied migration 16 is untouched
- 2026-09-24 · The Flex virtual-wallet SMS thread is not forwarded (not allow-listed) · it carries OTPs in the same thread
- 2026-09-24 · e2e specs use their own name prefixes for cleanup (`E2E-SMS …`) · money.spec's cleanup pattern `E2E …<project>-…` deleted the SMS spec's accounts mid-run when both ran in parallel
