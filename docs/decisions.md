# Decisions log

Format: date · decision · why. Append-only.

- 2026-09-23 · Project name **Gedara (ගෙදර)**; slug `gedara` everywhere · final name
- 2026-09-23 · Backend Supabase on a **separate account (ayeshmantha1991.24@gmail.com)**, projects `gedara-staging` + `gedara-prod`, region Singapore · main account is used by another project; closest region
- 2026-09-23 · Hosting Vercel on `gedara.vercel.app` (fallback `gedara-home`), **no custom domain** · free; QR plan in MASTER_PLAN §7d
- 2026-09-23 · Household model; users Didula Ayeshmantha (ayeshmantha@gmail.com, owner) and Sandeepani Thennakoon (asithaathennakoon@gmail.com, member); public sign-up disabled · two-person household with invite-only access
- 2026-09-23 · Auth = email 6-digit OTP (not magic link) · magic links open Safari, not the installed iOS PWA
- 2026-09-23 · Labels: NIIMBOT B1 **20×20 mm** (raw code only, QR v1-M alphanumeric) + Epson L3110 A4 **6×9 = 54** (URL QR) · owned/purchased hardware
- 2026-09-23 · Photos: Supabase thumbnails + full size until ~70 % full; Google Drive overflow for large files/docs · stay within free tiers
- 2026-09-23 · Kitchen Scale Station on Raspberry Pi 2 (HX711 5 kg + USB scanner + NIIMBOT print service) as Phase 6b · exact consumption of loose goods
- 2026-09-23 · Auth emails sent via **Gmail SMTP** (ayeshmantha1991.24@gmail.com, App Password) in both projects · Supabase default SMTP only reaches org team members, 2/hour
- 2026-09-23 · Vercel Root Directory = `apps/web` · never publish docs/reference as static files
- 2026-09-23 · React 19 / Vite 8 / Tailwind 4 / TypeScript 6.0 instead of MASTER_PLAN's "React 18" · current shadcn/ui + TanStack target React 19; TS pinned < 6.1 because typescript-eslint doesn't support TS 7 yet
- 2026-09-23 · `app_meta` is the one table without `household_id` (RLS on, no policies, read only via `schema_version()`) · global build metadata, nothing household-specific
- 2026-09-23 · `schema_version` = integer bumped by the last statement of every migration · the badge shows exactly which migration a DB has
- 2026-09-23 · Service worker via vite-plugin-pwa **injectManifest** (own `src/sw.ts`), not generateSW · generateSW's precache is cache-first; ours is network-first with caches as offline fallback only
- 2026-09-23 · APP_VERSION = `apps/web/package.json` version, scheme `0.<phase>.<deploy>` · version tells you the phase at a glance
- 2026-09-23 · Tenancy tables are read-only to clients in Phase 0; membership only via `household_invite` + auth trigger · invite-only household
- 2026-09-23 · No Docker: SQL tests run against gedara-staging via `scripts/sql-test.mjs` → `supabase db query --linked` (Management API; each test ends by raising its TAP output, so the transaction always aborts — nothing persists; refuses unless linked ref = staging; `--with-migrations` proves new migrations before `db push`); e2e logs in with an admin-minted OTP (`generateLink`, no email) and refuses non-staging URLs · develop against staging, never prod
- 2026-09-23 · supabase-js pinned to 2.116.0 (not 2.117.1) · pnpm 12 minimum-release-age gate; don't bypass it for day-old releases
- 2026-09-23 · RLS helpers live in schema `private` (`private.is_member`, `private.can_write`, `private.is_owner`), moved there by migration 6 · Supabase advisor 0029: SECURITY DEFINER helpers shouldn't be callable as /rest/v1/rpc; cheaper to fix before dozens of policies exist
- 2026-09-23 · Accepted advisor warnings: `schema_version()` executable by anon/authenticated (intentional — badge shows DB version on the login screen) · leaked-password protection off (OTP-only, no passwords)
- 2026-09-23 · gedara-staging was created in **Tokyo (ap-northeast-1)**, prod is Singapore · staging-only latency cost; not worth recreating now
- 2026-09-23 · Email OTP Length set to **6** in both Supabase projects (was 8) · app + email template expect a 6-digit code; an 8-digit code was silently truncated
- 2026-09-23 · e2e runs on the system **Microsoft Edge** (`E2E_CHANNEL=msedge`) on the dev PC · Playwright's Chromium 1243 fails to start on this Windows 10 machine (side-by-side error)
- 2026-09-23 · Design language **Aurora** replaces the ledger v7 palette (void #05070F, glass, violet/cyan/teal aurora; gold #F5B83D kept for the single primary action) · approved from the design preview; CLAUDE.md rule 11 updated
- 2026-09-23 · User-selectable aurora themes (aurora, nebula, lagoon, ember, mono) + Motion (system/on/off), stored in Supabase Auth `user_metadata.prefs` and mirrored to localStorage for a flash-free first paint · per user, synced across devices, no new table/RLS; themes never change gold or status colours so meaning is stable
- 2026-09-23 · Real app shows empty states (ghost charts, "arrives in Phase N"), never sample numbers · a finance app must not display invented amounts
- 2026-09-23 · Appearance prefs carry `updatedAt`; on start-up the newer of device vs account wins and a newer local choice is re-pushed · a save interrupted by a reload/navigation would otherwise be lost or overwritten by stale account data
- 2026-09-23 · Web build order is `vite build && tsc -b --noEmit` · the TanStack Router plugin generates routeTree.gen.ts during the Vite build, so new route files are unknown to tsc until then
- 2026-09-24 · HL codes are generated by the database (`private.new_hl_code`, Crockford base32 from `gen_random_bytes`), unique across all households and immutable (no client INSERT/UPDATE privilege on `code`) · printed labels must never change meaning or collide between households
- 2026-09-24 · A place's parent must be in the same household via a composite FK `(household_id, parent_id) → location(household_id, id)`; cycles rejected by trigger; `path` kept by triggers · the tree can't be corrupted by the client
- 2026-09-24 · Place photos live in the general `attachment` table (one primary per entity), not a `location.photo_path` column · §3.7 wants one table for every file; Phase 5 reuses it for warranties/manuals
- 2026-09-24 · Deleting a place: the DB refuses while anything is inside; the UI hides it at once and deletes after an 8 s Undo window (committed immediately if the app goes to the background) · §5.4 Undo without a soft-delete column
- 2026-09-24 · NIIMBOT labels in Phase 1 = downloaded 1-bit 203 dpi PNGs printed from the NIIMBOT app; direct printing waits for the Pi station (6b) · §7c fallback, no extra hardware now
- 2026-09-24 · Camera scanning uses native BarcodeDetector where available, else zxing-wasm with its `.wasm` bundled by Vite (never jsDelivr); WebP encoding falls back to @jsquash/webp WASM (Safari) · works on iPhone/Windows, offline-capable, no third-party CDN
- 2026-09-24 · A4 label calibration is stored per household in `label_profile` (defaults in code until saved) · feed offsets belong to the printer, shared by both users
- 2026-09-24 · Label-PDF code (pdf-lib + fontkit, ~1 MB) is a lazy chunk excluded from the service-worker warm-up · don't make every phone download it on each deploy
- 2026-09-24 · iPhone camera QR scans open Safari, not the installed PWA (same as magic links): sign in once in Safari and labels open straight to the place; Android opens `/s/` links in the installed app · platform limitation; A4 labels still work everywhere
- 2026-09-24 · Login keeps a validated same-origin `redirect` (`/login?redirect=/s/…`) · a scanned label must land on the box after signing in
- 2026-09-24 · Print real labels from **production** after merge: A4 QR URLs point at `VITE_PUBLIC_BASE_URL` (gedara.vercel.app → prod DB); preview/staging codes don't exist in prod · labels are permanent
- 2026-09-24 · Phase 2 scope = Money core only; bank-SMS import is Phase 2b · keep the ledger migration and SMS parsing separately reviewable
- 2026-09-24 · Money tables are read-only to clients; every write goes through SECURITY DEFINER RPCs (`rpc_save_transaction`, `rpc_import_bills`, `rpc_delete_transaction`, `rpc_move_transactions`) that check `private.can_write` · header + lines are written atomically and "lines add up to the total" is enforced in one place (advisor 0029 accepted for these, as for the stock RPCs to come)
- 2026-09-24 · Balances are never stored: `v_account_balance` = opening balance at the END of `opening_on` + transactions after it; cards carry a negative balance (= owed), available = limit + balance; later corrections are `adjustment` transactions (new type) · rule 1; history before an account's opening day counts as spending but not towards its balance
- 2026-09-24 · Fingerprint prefixes: `b` scanned bills (ledger v7, byte-for-byte), `L` older Sheet rows without v7 ids (hash of their row ids; lines keep the Sheet id), `m` manual entries (hash of type|date|time|account|payee|total, `~n` when saved again on purpose), transfer fees `<transfer>~fee` · deterministic everywhere (rule 5); a double tap asks "save another?"
- 2026-09-24 · `transaction_line.line_no` is 0-based = the ledger v7 line index · the index is part of the v7 line fingerprint
- 2026-09-24 · A scanned bill whose items don't add up to its total gets one extra line (discount / rounding / charges) in the biggest line's category · "every rupee lives in transaction_line"
- 2026-09-24 · Ledger Sheet migration runs in the app (Money › Import › Old ledger Sheet, CSV → `rpc_import_bills` as the signed-in user) instead of `scripts/import-sheet.ts` · no service-role key needed for prod, a click for Didula, re-running is harmless
- 2026-09-24 · Sheet import skips the 2 `TEST-` rows and the second upload of Invoice 299 (Cargills 27 Jun, Rs 595.00 counted twice in the Sheet); reconciliation shows Sheet 204,613.61 = Gedara 204,018.61 + skipped 595.00 · the Sheet itself double-counted that bill
- 2026-09-24 · Sheet rows paid by "card" go to the suspense account "Card — to be matched"; moved to the right card by hand (bulk "Move") now, by SMS matching in 2b · the Sheet never said which card
- 2026-09-24 · System `unit` table (g, kg, ml, L, pcs, pack …, read-only, `household_id` null) created in Phase 2 instead of 3; a line trigger fills `unit_id`, `base_qty`, `price_per_base` · rule 4 from the first bill (Rs/kg for vegetables); Phase 3 adds household units + product conversions
- 2026-09-24 · Seeded for the Gedara household: ledger v7 categories + Income (Salary, Private practice, Interest, Gifts received, Other income) and the accounts Cash, BOC Savings ..319 (Rs 6,443.81 at end of 2026-09-24), BOC/Seylan/Sampath/People's credit cards with limits, Card — to be matched · from Didula's notes; card and cash starting balances are set in the app
- 2026-09-24 · Budgets are not in any §9 phase yet; the Home "Budget burn" card stays an empty state · propose them with Phase 6 Insights
- 2026-09-24 · Bank SMS arrive via the open-source Android app **SMS to URL Forwarder** (F-Droid, MIT) posting to the `sms-ingest` Edge Function with a per-device secret header · sender regex + text regex on the phone, custom headers, retries; no Play-Store SMS-permission issues, no paid gateway
- 2026-09-24 · Every alert lands in a **review inbox** (`sms_message`), never straight into the ledger; one-tap "Accept clear matches" only for exact links and transfer pairs · sender ids can be spoofed and parsers can be wrong; money entries must be confirmed by a person
- 2026-09-24 · OTP / PIN / password texts are dropped on the phone, in the browser (backup import), in the Edge Function and again in SQL (`private.sms_is_secret`); they are never stored, only counted · defence in depth for the one kind of SMS that must never leave the phone
- 2026-09-24 · Device tokens: 32 random bytes shown once, only sha256 stored, column not readable by clients; owner-only add/remove; the device path is the only place the service role is used (one RPC granted to service_role only) · the phone has no Supabase session; a leaked token can only add inbox rows and is revoked in one tap
- 2026-09-24 · Inbox fingerprint `s<djb2(sender|body|received minute)>`; the same sender + text within 24 h is also a duplicate; Seylan txn ids are unique · the live forwarder and a later SMS-backup file stamp the same SMS seconds apart; rule 5 (deterministic)
- 2026-09-24 · Inbox status is derived (ignored / has transaction_id / new), not stored; deleting a transaction returns its alerts to the inbox (FK on delete set null) · no status can drift from the ledger
- 2026-09-24 · Alerts map to accounts by `account.institution` + `last4` (BOC savings "319" vs BOC card "8873" share the sender) · no separate sender-mapping table; fixed in account settings
- 2026-09-24 · Matching suggestions are computed in the web app (`lib/sms/match.ts`, unit-tested); the database only enforces household + "not reviewed twice" (GDLNK) · rules will be tuned with real data; keeping them out of migrations keeps them cheap to change
- 2026-09-24 · Bank parsers live once in `supabase/functions/_shared/sms/parse.ts` (plain TS, no imports) and the web app imports them via the `@sms` alias · one parser for phone, backup and tests
- 2026-09-24 · `rpc_save_transaction` became a wrapper around `private.save_transaction(p, source)` (migration 20, create or replace) so SMS posts are `source = 'sms'` · same checks/fee logic for manual and SMS entries; the applied migration 16 is untouched
- 2026-09-24 · The Flex virtual-wallet SMS thread is not forwarded (not allow-listed) · it carries OTPs in the same thread
- 2026-09-24 · e2e specs use their own name prefixes for cleanup (`E2E-SMS …`) · money.spec's cleanup pattern `E2E …<project>-…` deleted the SMS spec's accounts mid-run when both ran in parallel
- 2026-09-24 · **No Grocy import** in Phase 3; products are entered fresh (scan an unknown barcode → prefilled new product) · Grocy hadn't been used for 8+ months, its data is stale; MASTER_PLAN §8/§9 annotated
- 2026-09-24 · Stock = `stock_lot` + append-only `stock_movement`; `qty_remaining` is changed only by the movement trigger, movements only by the SECURITY DEFINER RPCs (`rpc_purchase/consume/open/transfer/inventory/set_lot_due/undo`); a trigger even refuses UPDATEs of journal rows · rule 1; invariant qty_remaining = Σ delta is tested
- 2026-09-24 · FEFO order everywhere = opened lots first, then earliest due (no date last), then oldest purchase; candidate lots are locked FOR UPDATE in that order · Grocy's default; cuts waste; two phones can't go negative
- 2026-09-24 · Opening or moving part of a lot splits it into a new lot (`split_from_id`); whole lots change in place with before/after state in `stock_movement.meta` · per-lot due dates stay right; undo can restore the old state
- 2026-09-24 · `rpc_undo(correlation)` reverses one action once, and refuses (GDUND) when a touched lot changed since (ordering by `stock_movement.seq`, not timestamps) · undo must never rewrite history out of order
- 2026-09-24 · Moving stock into a freezer place restarts its due date at today + `due_days_frozen` (when set); "Still fine +30 days" is an `edit` movement · §0.1 #3; both undoable
- 2026-09-24 · A product's stock unit is fixed once it has stock history (GDUNL); a unit's size/dimension never changes; 'other' units (pack, bottle) convert only through product pack sizes, looked up forwards or backwards · every lot is stored in the stock unit; the "$6 M" bug came from invisible conversions
- 2026-09-24 · Every product gets an immutable HL:PRD code (not only loose goods); one barcode maps to one product per household · any product can have a label; scans are never ambiguous
- 2026-09-24 · Manual "Add stock" never creates a money transaction; `stock_lot.transaction_line_id` exists for Phase 4 · bills stay the only source of spending, no double counting
- 2026-09-24 · Pantry status chips are computed in the web app (`lib/pantry/status.ts`, due soon = 5 days); SQL views give raw facts · thresholds cheap to tune
- 2026-09-24 · Swipe gestures on pantry cards deferred to Phase 7; cards have a visible −1 and a ⋯ menu · Didula's choice; no accidental use while scrolling
- 2026-09-24 · HL:LOT lot labels deferred (scanner says "comes later") · optional per §3.8; not needed for Phase 3's done-when
- 2026-09-24 · Place photo helpers generalised into `lib/photos.ts` (location + product) · rule 9 pipeline written once
- 2026-09-24 · Route search params that can look like numbers (`?new=<barcode>`, `?q=`) are accepted as string or number and turned into text · TanStack Router JSON-parses typed/shared URLs, so `?new=4792024000222` arrived as a number and the page errored
- 2026-09-25 · Bill-line → product matching runs in the web app (`lib/spine/match.ts`: barcode → learned name → trigram name score); the database only stores learned names (`product_alias`) · same as SMS matching: rules tuned with real bills stay out of migrations; `pg_trgm` waits for Phase 6 search
- 2026-09-25 · A name match is pre-selected only when clearly best (≥ 0.55, 0.05 ahead of the next, same top-level category); otherwise it is just suggested · one confirm must not quietly put "Salted peanuts" into "Salt"
- 2026-09-25 · Learned names are unique per household (`bill_name_norm`: lower-case, ASCII punctuation → spaces) and can also mean "not stock"; they are written only by the routing RPCs, removable on the product page · the next bill from the same shop needs no taps
- 2026-09-25 · A bill line becomes one lot: quantity in the line's unit (no unit printed → the product's purchase unit), unit cost = line amount ÷ stock quantity, bought on the bill date; bill discounts / rounding stay expense lines · rule 4; every rupee still lives in a line
- 2026-09-25 · New products from a bill line are created on the spot with the normal product form, not inside the import RPC · reuses the ≤ 5-field form; an abandoned import leaves at most an empty product
- 2026-09-25 · "Things" lines are marked `destiny = 'asset'` without an asset row until Phase 5 · the asset table doesn't exist yet; Phase 5 lists these as "bought, not entered yet"
- 2026-09-25 · Undo of an import = deleting the bill; deleting a bill undoes its purchases (rpc_undo) and re-opens the list items it ticked, refuses with GDUSE when the stock was used since, and can keep the stock instead · rule 1 (stock only moves through movements) and no orphaned "free" stock by accident
- 2026-09-25 · A bill whose lines feed the pantry keeps its lines: edits change the header only (`keep_lines`, GDRTD otherwise) · deleting and re-inserting lines would silently cut the lots' link to what they cost
- 2026-09-25 · One shopping list ("Main") for now; below-minimum products are added/removed by `rpc_shopping_sync` when the list, Pantry or Home opens (not by a stock trigger); editing an automatic item makes it the user's; "Not now" hides it until new stock arrives · no trigger in the hot stock path, and the user stays in control
- 2026-09-25 · The shopping list is live between phones (Realtime on `shopping_list_item`) · two people in the same shop
- 2026-09-25 · Prices by shop come from lots bought on bills (`v_product_price`, Rs per stock unit); a taken-back purchase is never a price · §5.2 price per kg by merchant, §4 row 4 cheapest recent merchant
- 2026-09-25 · A pantry line with no product picked doesn't block the import: it is saved as a plain bill line (no lot, nothing learned) and can be sent to the pantry later from the bill page; only a picked product whose quantity can't be converted blocks · a first bill with 30 new groceries must still be one confirm
- 2026-09-25 · e2e seeds pantry data as the signed-in test user (saved session), not the service role · row triggers call `private` helpers that only signed-in users may use (service_role has no USAGE on `private`)
- 2026-09-25 · **Google Drive overflow deferred** (Didula's choice): every file stays in the private Supabase bucket (photos as WebP, PDFs ≤ 10 MB); Settings › Storage shows the use against the free 1 GB and says when to set Drive up (~70 %). `attachment.provider / drive_file_id` already exist, so Drive needs no schema change later · one household fills 1 GB slowly; no Google OAuth setup now
- 2026-09-25 · Clothing, Footwear and Gifts are "expense only" by default; their Things lines not yet entered were moved to expense only (migration 38) · Didula's choice: 13 of the 26 queued prod lines were clothes nobody tracks as assets
- 2026-09-25 · "Bought, not entered yet" is a query (`v_asset_pending_line`: Things lines no asset points at), not auto-created draft assets as §4 row 1 suggested; "Not a thing" re-routes the line to expense only (Undo routes it back) · no junk rows for historical lines; one tap either way
- 2026-09-25 · The thing points at its bill line (`asset.transaction_line_id`, §3.5), not the other way round; a trigger copies price (line amount ÷ line qty × asset qty), date and shop when not typed, and refuses non-Things lines (GDLIN) · one line can buy several things; deleting the bill later only removes the link
- 2026-09-25 · Bill lines with things are protected by triggers on `transaction_line` (delete while the bill still exists, or re-routing away from Things → GDRTD); the web app edits such bills header-only · works for every write path without replacing `private.save_transaction` again
- 2026-09-25 · Asset label code = random `HL:AST:XXXXXX` like places/products; the human tag A-0042 is a separate per-household number from a counter table, never reused (§3.8's `HL:AST:A0042` example not used) · codes stay unique across households and unguessable; a printed A-number never names a different thing
- 2026-09-25 · Asset CRUD is plain table writes (RLS + column grants); money goes through RPCs only: `rpc_log_maintenance` (create the expense or link an existing one), `rpc_asset_sell` / `rpc_asset_unsell`, `rpc_asset_split`; `sold` can't be set or cleared by hand (GDSLD); deleting a sale's income in Money brings the thing back · rule "every rupee lives in transaction_line" with no way for asset and ledger to disagree
- 2026-09-25 · A service can be linked to an expense already in Money (SMS / scanned) instead of creating one · a card-paid technician would otherwise be counted twice
- 2026-09-25 · Value is computed, never stored: straight-line depreciation to the salvage value over the useful life (whole months like Postgres `age()`), cost of ownership = price + services − sale price; useful-life suggestions per category live in the web app · no stale numbers; tunable without migrations
- 2026-09-25 · Insurance is `insured` + notes for now; §3.5's policy-as-asset link waits for Phase 6 renewals · not needed for the done-when
- 2026-09-25 · New generic `activity` table (§3.7) written only by triggers; Phase 5 records the asset timeline (created / moved / lent / returned / sold / unsold / status) · Phase 6 extends it to the Attention feed instead of adding another log
- 2026-09-25 · A bill's receipt shows on every thing bought on it by reading the bill's attachments ("From the bill"), not by copying rows as `kind='warranty'` (§4 row 8) · one file, can't drift
- 2026-09-25 · Phase 6 scope (Didula): Web Push in this phase on Android Chrome, laptop and iPhone/iPad Home-Screen app; budgets monthly per **main** category, carrying over; **floor plan deferred to Phase 7**; personal inflation from **our own basket only** (no CCPI entry) · keeps the phase to one review; no monthly manual CCPI typing; data only goes back to May 2026
- 2026-09-25 · Recurring bills never post on their own: Pay (saves + links in one RPC) or link an SMS / scanned bill; due dates are computed from `first_due` + the latest paid / skipped period (`v_recurring_due`), never stored · money entries are confirmed by a person (2b); deleting a payment makes the bill due again with nothing to keep in sync
- 2026-09-25 · Insurance renewals are recurring rules with an optional thing (`recurring_rule.asset_id`) instead of a policy-as-asset · one mechanism for every "due" money event; the Phase 5 note said renewals wait for Phase 6
- 2026-09-25 · Units (kWh / m³) are typed with the payment (`meter_reading.units` per bill), not as meter readings · the bill prints units used; Rs per unit = bill ÷ units
- 2026-09-25 · Budget for month M = the latest `budget` row on or before M (0 = none from then); plain table writes under RLS · set once, carries over; budgets aren't balances (rule 1 doesn't apply)
- 2026-09-25 · The Attention feed is ONE SQL definition (`private.attention_items`) used by the app (`attention_feed`) and the push digest; thresholds (5 days due soon, 30 days warranty, notify_days_before) live there; items carry a deterministic `item_key` that changes with the situation, so "hide until it changes" is a dismissal without an end date · the 07:00 notification and Home can never disagree
- 2026-09-25 · Web Push is implemented with WebCrypto only (`supabase/functions/_shared/push`: RFC 8291 aes128gcm + RFC 8292 VAPID), unit-tested by decrypting in vitest · no third-party push library to trust or keep compatible with the Edge runtime
- 2026-09-25 · VAPID keys: one pair per project, only in Edge Function secrets (`scripts/vapid-keys.mjs` makes a pair and sets the secrets itself through the CLI; the private key is never printed); the browser gets the public key from `attention-push` GET · nothing in apps/web or Vercel (rule 8)
- 2026-09-25 · The daily job is `pg_cron` 01:30 UTC (= 07:00 Asia/Colombo) → `net.http_post` with a random token the migration stores in Vault; `rpc_push_digest` / `rpc_push_result` are service_role-only AND check that token; the function URL comes from a Vault secret `project_url` set once per project · the endpoint is public (verify_jwt off), so only the database's own job may start a run; nobody handles the token
- 2026-09-25 · One summary per household per day (`push_run` claims the day), sent only when the feed isn't empty; dead endpoints (404/410) are removed · a morning nudge, not a stream of alerts
- 2026-09-25 · Push text is English inside the Edge Function (`_shared/push/digest.ts`); the app's strings stay in i18n · the server has no user locale yet; Sinhala later
- 2026-09-25 · Insights are live security_invoker views + `insights_spend()` (grouped, because PostgREST returns ≤ 1000 rows); no materialized views · one household's data is small; MVs can't use RLS and can go stale
- 2026-09-25 · Drill contract in the URL: `/insights/<area>?from&to&cat&sub&product|name&merchant&account&kind&recurring&weekday&hour&by&view`; the level follows from which params are set; the record is `/money/tx/<id>?line=<lineId>` or `/pantry/journal?product&reason&from&to`; amounts in Insights go through `<Num to=…>` whose `to` is required · "every number clickable" enforced by the type checker
- 2026-09-25 · Charts are hand-made (BarList, Columns, LineChart, Heatmap), one or two series, every mark a link and every value also shown as text · no chart library in the bundle; no colour- or hover-only information
- 2026-09-25 · Activity: one row per stock action (upsert on correlation), a deleted transaction keeps a single "deleted" entry, maintenance logs are their own entity (the asset page already lists them) · a readable timeline instead of one row per movement
- 2026-09-25 · ⌘K search is `search_all()` over trigram-indexed names (product incl. Sinhala + barcodes, asset maker/model/serial/A-number, place path, bill payee/invoice/notes, bill lines, categories) · one query, typo-tolerant (`word_similarity` ≥ 0.45)
